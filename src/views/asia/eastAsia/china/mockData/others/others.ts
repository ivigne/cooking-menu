/**
 * 未知省份-未知城市-美食
 */

// import { groupBy, handleNames } from '/@/utils/general';

// import { lampshadedBeefScriptsList } from './templateFoodScriptContent/lampshadedBeef';
export const foodsStr = '';
export const othersFoodsList = [];
console.log(
  '其他省-未知城市的菜： ',
  othersFoodsList.length,
  // othersFoodsList.map((item) => item['foodName']),
);
// const list = await groupBy(othersFoodsList);
// console.log('groupBy(othersFoodsList)：', list);
// console.log('handleNames(list)：', handleNames(list, 'city', 'foodName', '', true));
// console.log(
//   'groupedObject(othersFoodsList)：',
//   othersFoodsList?.groupBy((item) => {
//     return hanziToPinyin(item['province']);
//   }),
// );
